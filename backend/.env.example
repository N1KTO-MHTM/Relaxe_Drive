# RelaxDrive Backend
PORT=3000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database (SQLite — no install. For production use PostgreSQL.)
DATABASE_URL="file:./prisma/dev.db"

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Redis (sessions, pub/sub)
REDIS_URL=redis://localhost:6379

# Optional: Maps (OpenRouteService), AI ETA, Translation (LibreTranslate)
# Without keys services return stub/fallback values.
# OPENROUTE_API_KEY=     # https://openrouteservice.org — routes & ETA
# AI_ETA_URL=            # Optional: POST { orderId, from, to } → { minutes } for AI ETA
# LIBRETRANSLATE_URL=    # Default: https://libretranslate.com
# LIBRETRANSLATE_API_KEY= # Required for libretranslate.com; optional for self-hosted
