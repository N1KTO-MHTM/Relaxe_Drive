# RelaxDrive Backend
PORT=3000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database (SQLite — no install. For production use PostgreSQL.)
DATABASE_URL="file:./prisma/dev.db"

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Redis (sessions, pub/sub)
REDIS_URL=redis://localhost:6379

# Optional: planning and reminders
# PICKUP_REMINDER_MINUTES=15   # Remind about pickups in the next N minutes (cron every 5 min)

# Optional: Maps (OSRM + Nominatim by default; no keys required)
# OSRM_URL=              # Default: https://router.project-osrm.org (or your OSRM server)
# GOOGLE_MAPS_API_KEY=    # If set, routes and ETA use Google Directions (traffic-aware); same data is drawn on our map
# AI ETA, Translation (LibreTranslate)
# AI_ETA_URL=            # Optional: POST { orderId, from, to } → { minutes } for AI ETA
# LIBRETRANSLATE_URL=    # Default: https://libretranslate.com
# LIBRETRANSLATE_API_KEY= # Required for libretranslate.com; optional for self-hosted
