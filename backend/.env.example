# RelaxDrive Backend
PORT=3000
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database (SQLite — no install. For production use PostgreSQL.)
DATABASE_URL="file:./prisma/dev.db"

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Redis (sessions, pub/sub)
REDIS_URL=redis://localhost:6379

# Optional: Maps, AI, Translation (for graceful degradation if missing)
# MAPS_API_KEY=
# AI_ETA_URL=
# TRANSLATION_API_KEY=
