{
  "name": "relaxdrive",
  "version": "1.2.0",
  "private": true,
  "description": "RelaxDrive — Control Center Platform",
  "scripts": {
    "backend": "npm run start:dev --prefix backend",
    "web": "npm run dev --prefix web",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix web",
    "build:all": "npm run build --prefix backend && npm run build --prefix web",
    "test": "npm run test --prefix web",
    "typecheck": "npm run build --prefix web",
    "build:web": "npm run build --prefix web",
    "deploy:frontends": "npm run build --prefix web",
    "check-locales": "npm run check-locales --prefix web",
    "db:generate": "npm run prisma:generate --prefix backend",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:studio": "npm run prisma:studio --prefix backend"
  }
}
