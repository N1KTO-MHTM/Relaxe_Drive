{
  "name": "relaxdrive",
  "version": "1.2.0",
  "private": true,
  "description": "RelaxDrive — Control Center Platform",
  "scripts": {
    "backend": "npm run start:dev --prefix backend",
    "web": "npm run dev --prefix web",
    "desktop": "npm run dev --prefix desktop",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix web && npm install --prefix desktop",
    "build:all": "npm run build --prefix backend && npm run build --prefix web && npm run build --prefix desktop",
    "test": "npm run test --prefix web",
    "typecheck": "npm run build --prefix web",
    "build:web": "npm run build --prefix web",
    "build:desktop": "npm run build:win --prefix desktop",
    "build:web-and-desktop": "npm run build --prefix web && npm run build:win --prefix desktop"
  }
}
