# Что осталось сделать — Relaxe Drive

## Уже сделано

- Репозиторий на GitHub, код запушен
- Blueprint на Render: **relaxdrive-db**, **relaxdrive-api**, **relaxdrive-web** — задеплоены
- Веб: https://relaxdrive-web.onrender.com | API: https://relaxdrive-api.onrender.com
- Роли и вкладки по доступу (Admin / Dispatcher / Driver), создание заказов на Dashboard, смена ролей на странице «Роли»
- Seed: Admin, Disp1, Driver1 (пароль **Luka1Soso**), тестовый заказ — выполнить в Render Shell: `npm run prisma:seed`
- **[x] Seed на Render и вход Admin** — выполнено пользователем
- **[x] Сменить пароль админа** — выполнено пользователем
- **Полный цикл заказа** — статусы SCHEDULED → ASSIGNED → IN_PROGRESS → COMPLETED, назначение водителя в UI, кнопки «В путь» / «Завершить» на Dashboard
- **WebSocket** — подписка на `orders`, обновление списка в реальном времени при создании/назначении/смене статуса
- **Календарь** — день/неделя, выбор даты, заказы по диапазону (GET /orders?from=&to=)
- **Карта в UI** — OpenStreetMap (Leaflet) на Dashboard, подсказка про маркеры при появлении координат
- **Forgot password** — запрос логируется; админ сбрасывает пароль через страницу «Роли» → «Сбросить пароль» (PATCH /users/:id/password)
- **Retry + Circuit Breaker** — для Geo (OpenRouteService) и Translation (LibreTranslate): 3 повтора с backoff, после 5 ошибок цепь открыта 60 сек
- **Desktop .exe** — `npm run build:win` в папке desktop, установщик в `dist-electron`; в `.env` указать прод API (см. [DESKTOP.md](./DESKTOP.md))
- Документация: ARCHITECTURE.md, DEPLOY-RENDER.md, **ABOUT.md**, **DESKTOP.md**, UI-LAYOUTS.md, **INTEGRATIONS.md**, **ROADMAP.md**, **CHECKLIST-REQUIREMENTS.md**

**Не входится Admin?** → см. раздел «Не могу войти (Admin / Luka1Soso)» в [DEPLOY-RENDER.md](./DEPLOY-RENDER.md).

---

## Что осталось / по желанию

### По желанию

- [ ] **Driver App** — PWA или мобильное приложение (принять/отклонить заказ, GPS, ETA).
- [ ] **Свой домен** в Render для web и API.
- [ ] **Бэкапы БД**, мониторинг и алерты.

### Расширенный roadmap (интеллект, карты, контроль)

Полный список идей: **AI copilot диспетчера**, предиктивные окна времени, умный буфер, multi-route, геозоны, **Incident Center**, Chaos-режим, **Live Wall Pro**, session heatmap, контекстный перевод, silent mode, аналитика «почему», Replay Mode, цветовая семантика, **Drag & Drop** диспетчеризация + доп. идеи — см. **[ROADMAP.md](./ROADMAP.md)**.

---

## Краткий чек-лист

- [x] Репозиторий, деплой API + БД + веб
- [x] Роли, меню по ролям, создание заказов, смена ролей (Roles)
- [x] **Seed на Render и войти Admin**
- [x] Сменить пароль админа в проде
- [x] Полный цикл заказа, WebSocket, календарь, карта, сброс пароля админом, Retry/CircuitBreaker, десктоп exe
- [ ] Driver App / домены / бэкапы (по желанию)

Подробнее о системе — [ABOUT.md](./ABOUT.md). Как запустить Desktop — [DESKTOP.md](./DESKTOP.md).
